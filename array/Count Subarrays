#include <bits/stdc++.h> 
int numberofSubarrays(vector<int> & arr, int n){
   int ans=0;
   int i=0;
   while(i<n){
   int j=i;
   while(j<n && arr[i]==arr[j]){
       j++;
   }
   int val=j-i;
   ans+=val*(val+1)/2;
   i=j;
   }
   return ans;
}
