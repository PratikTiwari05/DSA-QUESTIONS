#include <bits/stdc++.h> 
long long titleToNumber(string str) {
   long long result=0;
   for(int i=0;i<str.size();i++){
       char ch=str[i];
      long long val=ch-'A'+1;
      result=result*26+val;
   }
   return result;
}
