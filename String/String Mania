#include <bits/stdc++.h> 
int stringMania(int n, int m, string str1, string str2) {
    if(str1==str2){
        return 0;
    }
    int val= min(n,m);
    for(int i=0;i<val;i++){
         if(str1[i]>str2[i]) return 1;
         if(str1[i]<str2[i]) return -1;
    }
    if(n==m) return 0;
    if(n>m) return 1;
    return -1;
}
